import axios from 'axios';
import { API_CONFIG } from '../../config/config';

// Este archivo debe contener la implementación del cliente HTTP para realizar
// peticiones a la API FastAPI hosteada en OpenCloud.
// Aquí se deben implementar los métodos para realizar peticiones GET, POST, PUT, DELETE
// utilizando axios con la configuración importada desde config.ts

export const apiClient = axios.create({
  // La implementación completa debe ser añadida aquí
});